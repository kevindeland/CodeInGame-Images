from PIL import Image, ImageDraw

# Load image:
input_image = Image.open("img/vaccines.png")
input_pixels = input_image.load()

## for (var x=0; x < input_image.width; x += square)
## for (var y=0; y < input_image.height; y ++ square)

#### create output_image with size (square, square)
#### create draw = ImageDraw.Draw(output_image)

#### Copy Pixels
#### Save new thing
